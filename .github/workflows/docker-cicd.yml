name: Docker CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # Updated to v3 for better support

      - name: List files to check for index.html
        run: ls -al sample-app/  # Check files in the sample-app directory

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v3  # Updated to v3 for better support
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Check buildx builder
        run: docker buildx ls

      - name: Build Docker image
        run: |
          docker buildx build --platform linux/amd64 -t albinfrancis12/my-app:latest -f ./sample-app/Dockerfile ./sample-app --push

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Deploy to EC2 instance via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}  # Replace with your EC2 instance public IP
          username: ${{ secrets.EC2_USERNAME }}  # Replace with your EC2 username (e.g., "ubuntu" or "ec2-user")
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}  # Replace with your SSH private key
          script: |
            docker pull albinfrancis12/my-app1:latest
            docker stop my-container || true
            docker rm my-container || true
            docker run -d --name my-container -p 80:80 albinfrancis12/my-app1:latest